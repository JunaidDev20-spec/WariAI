# Data

Source videos are stored in `Videos/` at the repository root.

`Videos/` is gitignored because video files are large binary assets that should not be stored in Git. Place your input videos there before running the pipeline.

Do not modify, overwrite, rename, or re-encode files inside `Videos/`.
